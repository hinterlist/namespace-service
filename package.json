{
  "name": "namespace-service",
  "version": "1.0.0",
  "main": "dist/server.ts",
  "license": "MIT",
  "scripts": {
    "start": "nodemon --config nodemon.json",
    "start:debug": "nodemon --config nodemon-debug.json",
    "build": "tsc",
    "spin": "docker-compose up -d",
    "nuke": "docker-compose down -v && docker-compose up -d"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "ts-node": "^8.6.2",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.2"
  }
}
